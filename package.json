{
  "name": "canvasbuilder",
  "private": true,
  "scripts": {
    "dev": "bun --filter example dev",
    "dev:core": "bun --filter @jiant/canvable dev",
    "dev:app": "bun --filter example dev",
    "build": "bun --filter '*' build"
  },
  "workspaces": [
    "packages/*",
    "apps/*"
  ]
}